---

- name: checking variables using files
  hosts: localhost
  vars_files:
  - 03varfile.yaml
  become: yes
  tasks:
  - name: task1
    ansible.builtin.debug:
      msg: "established {{estd}}"
      