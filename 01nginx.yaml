---

- name: nginx installation and running
  hosts: localhost
  become: yes
  tasks:
  - name: install nginx
    ansible.bultin.packages:
      name: nginx
      state: present
  - name: start nginx
    ansible.builtin.package:
      name: nginx
      state: started    
